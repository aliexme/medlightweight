import { CLIENT } from 'types/client'

export const DEFAULT_PATIENT_SURVEYS_PAGE_SIZE = 5

export const DEFAULT_PATIENT_SURVEYS_INFO: CLIENT.PatientSurveysInfo = {
  surveysListIds: [],
  totalCount: 0,
}
